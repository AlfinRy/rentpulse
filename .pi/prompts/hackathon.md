---
description: Start or update the evidence-based Convex hackathon build log
argument-hint: "[start]"
---
Read `.agents/skills/convex-hackathon-skill/SKILL.md` completely, then read its
`references/log-format.md` and follow that skill directly.

Request: /hackathon ${ARGUMENTS:-update}

Work at this project's Git root. Keep Event exactly `Convex All Gas Hackathon`.
Preserve the user's confirmed frontend hosting choice. Backfill only supported
repository evidence. Never read secret stores or real environment files for the
log; never write secrets, personal information or database records into it.
Edit only `hackathon.md`. Do not build, deploy, commit, push, publish or submit.
